# =============================================================================
# roboarm-ai-toolkit — Development Dependencies
# Python 3.12 | Pinned versions for reproducibility
# =============================================================================
#
# Install: pip install -r requirements_dev_python312.txt
#
# Includes everything from requirements_python312.txt plus dev tools.
# For the full dev install via pyproject.toml: pip install -e ".[dev]"
#
# Platform: Windows 10/11 (also compatible with Linux and macOS)
# Last verified: 2025-07-24
# =============================================================================

# --- Core runtime (same as requirements_python312.txt) ---
numpy==2.5.1
matplotlib==3.11.1

# --- Testing ---
pytest==9.1.1                 # Test framework
pytest-cov==7.1.0             # Coverage reporting for pytest
coverage==7.15.2              # Code coverage measurement
iniconfig==2.3.0              # pytest config parsing
pluggy==1.6.0                 # pytest plugin system

# --- Linting & formatting ---
ruff==0.16.0                  # Fast Python linter and formatter

# --- Type checking ---
mypy==2.3.0                   # Static type checker
mypy_extensions==1.1.0        # mypy extension stubs
typing_extensions==4.16.0     # Backport of typing features

# --- Pre-commit hooks ---
pre_commit==4.6.1             # Git pre-commit hook framework
cfgv==3.5.0                   # Config validator (pre-commit dep)
identify==2.6.19              # File identification (pre-commit dep)
nodeenv==1.10.0               # Node.js environment (pre-commit dep)
virtualenv==21.7.0            # Virtual environment (pre-commit dep)
distlib==0.4.3                # Distribution utilities (virtualenv dep)
filelock==3.32.0              # File locking (virtualenv dep)
platformdirs==4.11.0          # Platform directories (virtualenv dep)
PyYAML==6.0.3                 # YAML parsing (pre-commit dep)

# --- Matplotlib sub-dependencies (pinned for completeness) ---
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
packaging==26.2
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
pathspec==1.1.1               # Gitignore-style pattern matching (ruff dep)

# --- Platform-specific ---
colorama==0.4.6               # ANSI color support on Windows
